# Point the frontend at the API gateway (single entrypoint), not the backend
# directly. The gateway routes /api/v1/auth/* to auth-svc and the rest to the
# backend.
VITE_API_URL=http://localhost:8080/api/v1
